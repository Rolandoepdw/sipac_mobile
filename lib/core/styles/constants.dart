import 'package:flutter/material.dart';

// Padding
const double lowPadding = 8.0;
const double mediumPadding = 16.0;
const double highPadding = 24.0;
const double veryHighPadding = 32.0;

// BorderRadius
const double lowRadius = 8.0;
const double mediumRadius = 16.0;
const double highRadius = 24.0;
const double veryHighRadius = 32.0;

// Duration
const Duration duration500 = Duration(milliseconds: 500);

// Curves
const Cubic fastOutSlowIn = Curves.fastOutSlowIn;

